import { ImageDragDirectiveDirective } from './image-drag-directive.directive';

describe('ImageDragDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new ImageDragDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
